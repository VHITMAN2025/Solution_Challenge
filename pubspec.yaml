name: voteshield
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.12.1
  barcode_scan2: ^4.1.7
  cupertino_icons: ^1.0.8
  firebase_ui_firestore: ^1.7.1
  cloud_firestore: ^5.6.5
  qr_code_scanner: ^0.6.1 # Ensure this line is present

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
